/**
 * @packageDocumentation
 * @module api.functional.stocks.almacen
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
//================================================================
import type { IConnection } from "@nestia/fetcher";
import { NestiaSimulator } from "@nestia/fetcher/lib/NestiaSimulator";
import { PlainFetcher } from "@nestia/fetcher/lib/PlainFetcher";
import typia from "typia";

import type { IStockAlmacen } from "../../../structures/common/IStockAlmacen";

/**
 * @controller StockController.listarPorAlmacen
 * @path GET /stocks/almacen/:almacenId
 * @accessor api.functional.stocks.almacen.listarPorAlmacen
 * @nestia Generated by Nestia - https://github.com/samchon/nestia
 */
export async function listarPorAlmacen(
  connection: IConnection,
  props: listarPorAlmacen.Props,
): Promise<listarPorAlmacen.Output> {
  return true === connection.simulate
    ? listarPorAlmacen.simulate(connection, props)
    : PlainFetcher.fetch(connection, {
        ...listarPorAlmacen.METADATA,
        template: listarPorAlmacen.METADATA.path,
        path: listarPorAlmacen.path(props),
      });
}
export namespace listarPorAlmacen {
  export type Props = {
    _almacenId: string;
  };
  export type Output = Array<IStockAlmacen>;

  export const METADATA = {
    method: "GET",
    path: "/stocks/almacen/:almacenId",
    request: null,
    response: {
      type: "application/json",
      encrypted: false,
    },
    status: 200,
  } as const;

  export const path = (props: Props) =>
    `/stocks/almacen/${encodeURIComponent(props._almacenId?.toString() ?? "null")}`;
  export const random = (): Array<IStockAlmacen> =>
    typia.random<Array<IStockAlmacen>>();
  export const simulate = (connection: IConnection, props: Props): Output => {
    const assert = NestiaSimulator.assert({
      method: METADATA.method,
      host: connection.host,
      path: path(props),
      contentType: "application/json",
    });
    assert.param("_almacenId")(() => typia.assert(props._almacenId));
    return random();
  };
}
